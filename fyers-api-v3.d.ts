declare module 'fyers-api-v3' {
    // Define the types for the module here as needed
    // For now, you can use `any` to suppress the error
    export function someFunction(params: any): any; // Example function

    // Example class
    export class FyersApi {
        constructor(config: any);
        public someMethod(params: any): any;
    }

    // You can add more specific types as needed
}
